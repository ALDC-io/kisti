# Ensure KiSTI session is set for aldc user on every boot
# Installed to /etc/tmpfiles.d/kisti-accountsservice.conf
# systemd-tmpfiles runs this before GDM starts

f /var/lib/AccountsService/users/aldc 0644 root root - [User]\nSession=kisti-session\nSystemAccount=false
